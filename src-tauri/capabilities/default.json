{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Minimal capability set for the main profile inventory window",
  "windows": ["main"],
  "permissions": ["core:default"]
}
